# LC0141 环形链表（Linked List Cycle）

## 题目大意
判断给定链表中是否存在环（某个节点的 `next` 指向链表中之前的节点）。

## 思路（快慢指针）
- 使用两个指针 `slow`、`fast`：
  - `slow` 每次走一步，`fast` 每次走两步；
  - 若存在环，则 `fast` 一定会在环内追上 `slow`；
  - 若 `fast` 或 `fast.next` 变为 `null`，说明无环。

## 复杂度
- 时间复杂度：O(n)。
- 空间复杂度：O(1)。

## 考点
- 快慢指针在链表环检测中的应用。
