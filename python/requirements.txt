# Python 浏览器启动器依赖
# 注意：此项目使用 Python 标准库，无需额外安装依赖
# tkinter - GUI 库（Python 自带）
# webbrowser - 浏览器控制（Python 自带）
# subprocess - 进程控制（Python 自带）

# 打包为 EXE 所需的依赖（可选）
# PyInstaller - 推荐使用
pyinstaller>=5.0.0

# 其他打包工具（可选）
# auto-py-to-exe>=2.0.0
# cx_Freeze>=6.0.0

