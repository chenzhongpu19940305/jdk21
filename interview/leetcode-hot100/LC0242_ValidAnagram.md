# LC0242 有效的字母异位词（Valid Anagram）

## 题目大意
判断两个字符串是否为字母异位词：由相同字母重新排列而成。

## 思路（计数数组）
- 若长度不同，直接返回 false。
- 使用长度为 26 的整型数组 `cnt`：
  - 遍历 `s`，对对应字符计数 `+1`；
  - 遍历 `t`，对对应字符计数 `-1`；
- 最后检查 `cnt` 是否全部为 0。

## 复杂度
- 时间复杂度：O(n)。
- 空间复杂度：O(1)。

## 考点
- 字符频率统计。
- 利用计数数组判断异位词。
