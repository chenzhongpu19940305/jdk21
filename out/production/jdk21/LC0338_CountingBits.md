# LC0338 比特位计数（Counting Bits）

## 题目大意
给定整数 `n`，返回 `[0..n]` 每个数的二进制中 1 的个数。

## 思路（DP：利用低位关系）
- 对任意 `i`：
  - `i >> 1` 为 `i` 去掉最低位后的数；
  - `i & 1` 为最低位是否为 1；
  - 因此：`bits(i) = bits(i >> 1) + (i & 1)`。
- 从 1 到 n 递推即可。

## 复杂度
- 时间复杂度：O(n)。
- 空间复杂度：O(n)。

## 考点
- 位运算与 DP 的结合。
